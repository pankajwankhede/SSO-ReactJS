# Local-friendly override (no Geode required)
spring:
  session:
    store-type: none

server:
  servlet:
    session:
      cookie:
        secure: false
        same-site: lax

sso:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:3000
